server:
  port: 8201

spring:
  application:
    name: FrontendService
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/

info:
  name: FrontendService

logging:
  path: logfile
